export const GALAXY_RESPAWN_CONFIG = {
  minRespawnDelayMs: 24 * 3600000,
  maxRespawnDelayMs: 48 * 3600000,
  respawnCheckIntervalMs: 10 * 60 * 1000,
  maxSpawnedNodes: 20,
  minDistanceFromCenter: 50,
  mapBounds: 400,
};