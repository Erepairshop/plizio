Options -Indexes

<IfModule mod_rewrite.c>
  RewriteEngine On
  RewriteBase /

  # LiteSpeed blocks _next/ directory by default on Hostinger
  # Serve _next/ requests from next_static/ folder instead
  RewriteRule ^_next/(.*)$ /next_static/$1 [L]
</IfModule>
