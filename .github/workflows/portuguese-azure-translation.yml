name: Portuguese Azure Translation

on:
  workflow_dispatch:
    inputs:
      limit:
        description: "Pending batches to translate; 0 means all"
        required: true
        default: "2"
      mode:
        description: "Translation queue"
        type: choice
        required: true
        default: "full"
        options: [full, missing-descadv, missing-citytips-structured]

concurrency:
  group: portuguese-azure-translation
  cancel-in-progress: false

jobs:
  translate:
    runs-on: [self-hosted, vps-plizio]
    timeout-minutes: 300
    steps:
      - uses: actions/checkout@v4

      - name: Build deterministic Portuguese queue
        run: |
          set -euo pipefail
          MODE="${{ inputs.mode }}"
          WORK=/home/erik/plizio-translations/pt-20260806
          [ "$MODE" != "full" ] && WORK="/home/erik/plizio-translations/pt-$MODE-20260806"
          SOURCE_REPO=/home/erik/plizio/current
          if [ ! -s "$SOURCE_REPO/data/_poi-url-index.json" ] || [ ! -s "$SOURCE_REPO/data/pois/PT.json" ]; then
            echo "Generated Plizio data is missing from $SOURCE_REPO" >&2
            exit 1
          fi
          mkdir -p "$WORK"
          python3 scripts/native-rollout/build-country-queue.py \
            --repo "$SOURCE_REPO" --output-dir "$WORK" \
            --language pt --country-slug portugal --pois-file PT.json \
            --mode "$MODE"
          cat "$WORK/summary.json"

      - name: Translate through Azure OpenAI
        env:
          BATCH_LIMIT: ${{ inputs.limit }}
        run: |
          set -euo pipefail
          MODE="${{ inputs.mode }}"
          WORK=/home/erik/plizio-translations/pt-20260806
          [ "$MODE" != "full" ] && WORK="/home/erik/plizio-translations/pt-$MODE-20260806"
          set -a
          source /home/erik/plizio-audits/poi-only-technical-20260731/.azure-openai.env
          set +a
          python3 scripts/native-rollout/run-azure-translation.py \
            --queue "$WORK/queue.json" \
            --output "$WORK/results.jsonl" \
            --limit "$BATCH_LIMIT" \
            --workers 4 \
            --rpm 9 \
            --language-name "European Portuguese"

      - name: Persist run summary
        if: always()
        run: |
          set -euo pipefail
          MODE="${{ inputs.mode }}"
          WORK=/home/erik/plizio-translations/pt-20260806
          [ "$MODE" != "full" ] && WORK="/home/erik/plizio-translations/pt-$MODE-20260806"
          python3 - "$WORK" <<'PY'
          import json, sys
          from pathlib import Path
          root = Path(sys.argv[1])
          queue = json.loads((root / "queue.json").read_text())
          rows = []
          if (root / "results.jsonl").exists():
              for line in (root / "results.jsonl").read_text(encoding="utf-8").splitlines():
                  try: rows.append(json.loads(line))
                  except json.JSONDecodeError: pass
          latest = {row.get("id"): row for row in rows}
          valid = [row for row in latest.values() if row.get("status") == "completed"]
          usage = {key: sum(int((row.get("usage") or {}).get(key, 0) or 0) for row in valid)
                   for key in ("input_tokens", "output_tokens", "total_tokens")}
          summary = {"batches": len(queue), "completed": len(valid), "errors": sum(row.get("status") == "error" for row in latest.values()), "usage": usage}
          (root / "run-summary.json").write_text(json.dumps(summary, indent=2) + "\n")
          queue_by_id = {batch["id"]: batch["items"] for batch in queue}
          sample = []
          for row in valid[-2:]:
              source = queue_by_id.get(row.get("id"), {})
              translated = row.get("translations") or {}
              sample.append({
                  "id": row.get("id"),
                  "items": [
                      {"key": key, "source": value, "translation": translated.get(key)}
                      for key, value in list(source.items())[:8]
                  ],
              })
          (root / "translation-sample.json").write_text(
              json.dumps(sample, ensure_ascii=False, indent=2) + "\n", encoding="utf-8"
          )
          print(json.dumps(summary))
          PY

      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: portuguese-azure-translation-status
          path: |
            /home/erik/plizio-translations/pt-20260806/summary.json
            /home/erik/plizio-translations/pt-20260806/run-summary.json
            /home/erik/plizio-translations/pt-20260806/translation-sample.json
            /home/erik/plizio-translations/pt-missing-descadv-20260806/summary.json
            /home/erik/plizio-translations/pt-missing-descadv-20260806/run-summary.json
            /home/erik/plizio-translations/pt-missing-descadv-20260806/translation-sample.json
          if-no-files-found: warn
